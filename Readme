### Converter

Данный скрипт конвертирует все файлы в папке, в которой находится, из "*.html" в "*.md".

Для конвертации скрипт использует библиотеку PyPandoc.

Для правильной работы скрипта, возможно надо будет установить ее на ПК. 
Как это сделать читай по ссылке https://pypi.org/project/pypandoc/

Так же может понадобится импорт:
`import os
import glob
import pypandoc
import shutil`

Скрипт создаст папку "Обработано" и в нее положит файлы в новом формате.
